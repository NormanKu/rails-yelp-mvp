<h1>Add a New Review</h1>

<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :rating %>
  <%= f.input :content %>

  <%= f.button :submit, "Create Review" %>
<% end %>
<%= link_to "Back to review", restaurant_path(@restaurant), class: "btn btn-primary" %>
